/**
 * @param {number[]} gas
 * @param {number[]} cost
 * @return {number}
 */
var canCompleteCircuit = function (gas, cost) {};

const gas = [1, 2, 3, 4, 5],
  cost = [3, 4, 5, 1, 2];
console.log("candy:", canCompleteCircuit(gas, cost));
